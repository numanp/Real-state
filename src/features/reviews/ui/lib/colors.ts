/** Gold used by the rating stars + agency rating chip. Kept here so the two
 *  reviews components share one source of truth (same hue as the agency
 *  verification badge). lucide icons need a hex, so this can't be a Tailwind
 *  token. */
export const AGENCY_GOLD = '#e2b340';
